# Accessibility Notes

- Search suggestions are keyboard navigable (↑/↓/Enter) and expose `role=listbox/option`.
- The status banner uses `aria-live="polite"` for network/offline messages.
- Controls have clear labels and `aria-pressed` for unit toggles.
- Color contrast uses a dark theme; verify with browser tools if you customize colors.
